{
  "name": "atomic-chat",
  "version": "1.0.0",
  "description": "Full-stack chat application with WebRTC calling and voicemail",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "install:all": "npm install && cd shared && npm install && cd ../server && npm install && cd ../client && npm install",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "server:build": "cd server && npm run build",
    "client:build": "cd client && npm run build",
    "shared:build": "cd shared && npm run build",
    "prisma:generate": "cd server && npx prisma generate",
    "prisma:migrate": "cd server && npx prisma migrate dev",
    "prisma:studio": "cd server && npx prisma studio"
  },
  "keywords": [
    "chat",
    "webrtc",
    "voicemail",
    "real-time"
  ],
  "author": "",
  "license": "MIT"
}
